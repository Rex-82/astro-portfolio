---
import Layout from '../layouts/Layout.astro';
import PageTitle from '../components/PageTitle.astro';
import CtaButton from '../components/CtaButton.astro';

const workWith = ['TypeScript', 'Next.js', 'NestJS', 'PostgreSQL'];
const learning = ['Rust', 'Go'];
const like = ['Linux', 'Docker', 'Neovim', 'Lists'];
---

<Layout
	title="Simone Ferretti"
	description="Full-Stack Developer building products end to end. TypeScript, Next.js, NestJS, PostgreSQL, GCP."
>
	<main>
		<header>
			<PageTitle title="Simone Ferretti" />
			<p id="cta"><span>full-stack developer</span></p>
		</header>
		<div id="about">
			<section id="presentation">
				<span>I don't like introductions.</span>
				<span>Let what I do speak for myself.</span>
			</section>
			<section>
				<h2>What I work with</h2>
				<ul>
					{workWith.map((e) => <li>{e}</li>)}
				</ul>
			</section>
			<section>
				<h2>What I am learning</h2>
				<ul>
					{learning.map((e) => <li>{e}</li>)}
				</ul>
			</section>
			<section>
				<h2>More things I like</h2>
				<ul>
					{like.map((e) => <li>{e}</li>)}
				</ul>
			</section>
		</div>
		<CtaButton label="Projects" destination="/projects/" />
	</main>
</Layout>

<style>
	main {
		padding: 1rem;
		width: 800px;
		max-width: calc(100% - 2rem);
		font-size: 16px;
		color: var(--color-accent);
		line-height: 1.6;
		display: flex;
		flex-direction: column;
		align-items: center;
	}

	header {
		display: flex;
		flex-direction: column;
		align-items: center;
		width: fit-content;
		letter-spacing: 0.125em;
	}

	#cta {
		font-size: 1.5rem;
		text-transform: uppercase;
		letter-spacing: 0.125em;
		align-self: flex-end;
		margin-top: -2.5em;
	}

	@media screen and (max-width: 768px) {
		#cta {
			font-size: 1.125rem;
		}

		#cta + p {
			font-size: 3rem;
		}
	}

	@media screen and (max-width: 400px) {
		#cta {
			font-size: 1rem;
		}
	}

	section {
		display: flex;
		flex-direction: column;
		padding: 1rem;
	}

	#presentation {
		grid-area: pres;
		align-self: center;
		flex-direction: row;
		column-gap: 0.325em;
		flex-flow: wrap;
		justify-content: center;
	}

	#presentation > span {
		text-wrap: nowrap;
	}

	section:nth-of-type(2) {
		grid-area: sec2;
	}

	section:nth-of-type(3) {
		grid-area: sec3;
	}

	section:nth-of-type(4) {
		grid-area: sec4;
	}

	section h2 {
		font-size: 0.875rem;
		text-transform: uppercase;
		letter-spacing: 0.125em;
		margin: 0;
		padding: 0;
		color: color-mix(in srgb, var(--color-accent-light) 70%, transparent);
	}

	section ul {
		list-style: none;
		margin: 0;
		padding: 0;
		margin-left: 0.5rem;
		padding-left: 0.5rem;
		border-left: 1px solid color-mix(in srgb, var(--color-accent-light) 70%, transparent);
	}

	#about {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		grid-template-rows: 1fr 2fr;
		grid-template-areas: 'pres pres pres' 'sec2 sec3 sec4';
		justify-items: center;
		border-radius: 7px;
		width: 100%;
		margin-top: -1em;
	}

	@media screen and (max-width: 768px) {
		#about {
			grid-template-columns: 1fr;
			grid-template-rows: 1fr repeat(3, auto);
			grid-template-areas: 'pres' 'sec2' 'sec3' 'sec4';
			justify-items: center;
			row-gap: 1em;
			margin-bottom: 2rem;
		}

		#presentation {
			justify-self: center;
		}

		section:nth-of-type(n + 2) {
			padding-top: 0;
			border-bottom: 1px solid var(--color-border);
		}

		section > h2 {
			font-size: 1rem;
		}

		section {
			align-items: center;
		}

		section ul {
			display: flex;
			align-items: center;
			column-gap: 1em;
			height: 2em;
			border-left: none;
			margin: 0;
			padding: 0;
		}
	}
</style>
