---
const { title } = Astro.props;
---

<h1>{title}</h1>
<style>
	h1 {
		font-size: clamp(28px, 3vw, 40px);
		font-weight: 500;
		line-height: 1.15;
		letter-spacing: -0.05em;
		margin: 0 0 24px;
	}
</style>
